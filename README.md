# Gokul Chandrasekaran site

Replace the existing HTML files with the versions in this folder. Upload `styles.css` alongside them in the repository root. The site is designed for GitHub Pages with a shared stylesheet.
